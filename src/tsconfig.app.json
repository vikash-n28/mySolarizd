{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "baseUrl": "./",
    "module": "es2015",
    "types": [
      // npm i --save-dev (hammer manager)
      "jasmine", "hammerjs"
    ]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
